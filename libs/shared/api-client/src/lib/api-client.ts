/**
 * Base path every operation in §2.3's table is mounted under.
 *
 * Placeholder contents until `ledgerline-api` exists and its OpenAPI schema can
 * drive generation. See the banner in `../index.ts`.
 */
export const API_BASE_PATH = '/api';
