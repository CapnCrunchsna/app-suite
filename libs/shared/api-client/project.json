{
  "name": "api-client",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/api-client/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:api-client"],
  "// targets": "to see all targets run: nx show project api-client --web",
  "targets": {
    "generate-client": {
      "//": "Regenerate src/ from apps/ledgerline-api/openapi.json (spec 2.2: this lib is generated, never hand-edited). Named generate-client rather than generate because `nx generate` is the CLI's own command.",
      "executor": "nx:run-commands",
      "dependsOn": ["ledgerline-api:build"],
      "outputs": ["{projectRoot}/src"],
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "node tools/generate-api-client.mjs"
      }
    }
  }
}
