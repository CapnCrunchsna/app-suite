{
  "name": "ui",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/ui/src",
  "projectType": "library",
  "prefix": "ui",
  "tags": ["scope:shared", "type:ui"],
  "targets": {
    "build": {
      "//": "ng-packagr, not tsc — README.md explains the silent failure that avoids. `@angular/build:ng-packagr` rather than `@nx/angular:package` because `@angular/build:unit-test` only accepts an `@angular/build` build target to read compiler options from; against the Nx executor it refuses with 'not supported', and this lib now has tests. Same ng-packagr underneath, same Angular Package Format out — the swap changes nothing about the artifact.",
      "executor": "@angular/build:ng-packagr",
      "dependsOn": ["^build"],
      "outputs": ["{projectRoot}/dist"],
      "defaultConfiguration": "production",
      "options": {
        "project": "libs/shared/ui/ng-package.json",
        "tsConfig": "libs/shared/ui/tsconfig.lib.json"
      },
      "configurations": {
        "production": {},
        "development": {}
      }
    },
    "test": {
      "//": "`lint` is deliberately absent so it is inferred from @nx/eslint/plugin rather than using the deprecated @nx/eslint:lint executor.",
      "executor": "@angular/build:unit-test",
      "options": {
        "watch": false
      }
    }
  }
}
